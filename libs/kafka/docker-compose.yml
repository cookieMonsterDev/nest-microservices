services:
  kafka:
    image: confluentinc/cp-kafka:8.3.1
    container_name: kafka
    env_file:
      - ./.env
    ports:
      - '3011:3011'
      - '3012:3012'
    networks:
      - kafka-net

networks:
  kafka-net:
    driver: bridge
