services:
  kafka:
    image: confluentinc/cp-kafka:7.8.0
    container_name: kafka
    env_file:
      - ./.env
    ports:
      - '3011:3011'
      - '3012:3012'
    networks:
      - kafka-net

networks:
  kafka-net:
    driver: bridge
